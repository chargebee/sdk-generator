package {{pkgName}}
{{#if responseImports}}{{{responseImports}}}{{/if}}

{{#each operations}}
{{#if operationResponses.subResponseClassName}}
type {{ operationResponses.subResponseClassName }} struct {
{{{ operationResponses.subResponseParams }}}
}
{{/if}}

type {{ operationResponses.className }} struct {
{{{ operationResponses.responseParams }}}
}
{{/each}}
